


接口  /getPhone   post 和 get 都支持


action="GET"
  获取数据
    page  limit 我就不多说了  主要是  status  (1 未审核 2审核未使用 3审核已使用)
    要给他显示  头像  让他们  可以审核  (审核过后的数据才可以被脚本取走使用 懂吧!)

    三个界面(未审核  审核-已使用  审核-未使用  分别对应我上面写的状态)
    未审核 后面跟上一个设置(审核按钮)(接口  action=="check" 参数 status (这个值你给 2))   主要显示:手机号 性别 年龄 头像(要显示的吧图片读出来 让审核员可以看到) 上传时间
    审核-已使用  (要多一个 nickname(被那部手机使用的!))
    审核-未使用  基本和 未审核一样

action=="ToLead" 这个是 post 请求 参数 file  上传    xlsx 文件 我已经转发给你  你可以 先清库测试   表示  whats_pp







